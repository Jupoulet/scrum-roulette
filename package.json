{
  "name": "scrum-roulette",
  "type": "module",
  "version": "1.0.0",
  "description": "Chose a scrum master for the day",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist && tsc && node --es-module-specifier-resolution=node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "julien picard",
  "license": "ISC",
  "dependencies": {
    "@notionhq/client": "^1.0.4",
    "@slack/web-api": "^6.8.1",
    "axios": "^0.27.0",
    "dotenv": "^16.3.1"
  }
}
